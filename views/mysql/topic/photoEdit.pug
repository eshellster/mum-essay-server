doctype html
html
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title 사진업로드

body
    artcle
      if topic
        h2= topic.title
        = topic.description
        h2= topic.name+' 사진수정'
    ul
      form(action='/topic/'+topic.id+'/photoEdit' method='post' enctype="multipart/form-data")
        p
          input(type='file' name='userfile' accept='image/*')
        p
          input(type='submit')
