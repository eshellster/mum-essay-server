extends ../frontLayout
block content
  artcle
    h2= '\"'+topic.name +'\"학생을 등록 삭제 하시겠습니까?'
    form(action='/topic/'+topic.id+'/delete' method='post')
      p
        input(type='submit' value='YES')
    a(href='/topic/'+rid) No
